import React from 'react';
import ModeratorPanel from '../components/ModeratorPanel'; // Обратите внимание на путь импорта

const ModeratorPage = () => {
    return (
        <div>
            <h1>Moderator Page</h1>
            <ModeratorPanel />
        </div>
    );
};

export default ModeratorPage;
